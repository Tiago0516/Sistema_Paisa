@model SistemaPaisa.Web.Models.WorkspacePageViewModel

<partial name="_ModuleGrid" model="Model.Grid" />

@section Scripts {
    <script>
        window.moduleWorkspace = {
            activeModule: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.ActiveModuleCode))
        };
    </script>
}
